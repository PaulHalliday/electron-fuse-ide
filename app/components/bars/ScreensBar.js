import React, { Component } from 'react';
import { Link } from 'react-router';
import styles from './Toolbar.css';

export default class ScreensBar extends Component {
  render() {
    return (
      <div>
        <div className={styles.container}>
          // alignment
          // for each screen, ctrl to go to screen
          // add new screen button
          // allow dragging/rearranging of screens...
        </div>
      </div>
    );
  }
}
